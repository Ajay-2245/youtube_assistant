# Eval dataset generation (ragas) — install in a SEPARATE venv from the main app.
# ragas 0.3.9 still imports the deprecated langchain_community.chat_models.vertexai
# path, which was removed in langchain-community>=0.4, so it must be pinned below 0.4.
# This conflicts with the main app's langchain>=1.0 stack (create_agent requires it),
# hence the separate environment.

ragas==0.3.9
langchain-community<0.4
langchain-google-vertexai
langchain-chroma
langchain-google-genai
langchain-core
langsmith
pandas
rapidfuzz